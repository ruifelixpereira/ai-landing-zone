# =============================================================================
# GitHub OIDC Setup Configuration
# Copy this file to .env and update the values
# =============================================================================

# Required: GitHub organization or username
GITHUB_ORG=myorg

# Required: GitHub repository name
GITHUB_REPO=ai-landing-zone

# Required: Azure Subscription ID
SUBSCRIPTION_ID=00000000-0000-0000-0000-000000000000

# Optional: Azure AD Application name (default: github-ai-landing-zone)
# APP_NAME=github-ai-landing-zone

# Optional: Azure region for resources (default: swedencentral)
# LOCATION=swedencentral

# =============================================================================
# Terraform State Storage Configuration (for setup-tfstate-storage.sh)
# =============================================================================

# Required for state storage: Globally unique storage account name (3-24 chars, lowercase)
TFSTATE_STORAGE_ACCOUNT=stmyorgtfstate

# Optional: Resource group for state storage (default: rg-tfstate)
# TFSTATE_RESOURCE_GROUP=rg-tfstate

# Optional: Container name for state files (default: tfstate)
# TFSTATE_CONTAINER=tfstate

# Optional: Service Principal Client ID to grant storage access
# If not set, will auto-detect from APP_NAME (default: github-ai-landing-zone)
# GITHUB_SP_CLIENT_ID=00000000-0000-0000-0000-000000000000
